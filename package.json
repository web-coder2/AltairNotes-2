{
  "name": "altairnotes-2",
  "version": "1.0.0",
  "description": "переписываю приложение записи Альтаира на TypeScript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd server && npm run dev",
    "client": "cd client && npx nuxt dev --port 5000",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "w3b-c0d3r",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
